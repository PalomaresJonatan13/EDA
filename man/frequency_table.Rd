% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frequency_table.R
\name{frequency_table}
\alias{frequency_table}
\title{Frequency tables for categorical variables}
\usage{
frequency_table(df, vars = NULL)
}
\arguments{
\item{df}{A data frame or tibble.}

\item{vars}{Unquoted tidy-select specification of columns to include.
Defaults to all non-numeric columns in \code{df}.}
}
\value{
A named list of tibbles, one per variable. Each tibble has columns:
\describe{
\item{level}{The factor or character level (including \code{NA}).}
\item{absolute}{Absolute count of observations at that level.}
\item{relative}{Relative frequency (proportion) summing to 1.}
\item{percent}{Percent frequency (0–100).}
}
}
\description{
\code{frequency_table()} computes, for each selected non-numeric (categorical)
variable, the counts and proportions of each level (including \code{NA}).
}
\examples{
# Frequencies of Species in iris
freq1 <- frequency_table(iris, Species)

# Frequencies for multiple factors in a custom data frame
df <- data.frame(
  gender  = sample(c("M", "F"), 100, TRUE),
  outcome = sample(c("yes", "no"),  100, TRUE)
)
freq2 <- frequency_table(df, c(gender, outcome))

}
