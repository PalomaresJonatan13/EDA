% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heatmap_cor.R
\name{heatmap_cor}
\alias{heatmap_cor}
\title{Pairwise Scatter Plots with Optional Grouping}
\usage{
heatmap_cor(cor_df, title = "Correlation heatmap", limits = c(-1, 1))
}
\arguments{
\item{df}{A data frame or tibble containing the data.}

\item{rows}{Optional unquoted tidy-select specification of numeric columns
to use as the \emph{y} variables.  Defaults to all numeric columns if \code{NULL}.}

\item{cols}{Optional unquoted tidy-select specification of numeric columns
to use as the \emph{x} variables.  Defaults to all numeric columns if \code{NULL}.}

\item{cat_var}{Optional unquoted name of a non-numeric (categorical)
column in \code{df}.  If provided, points are colored by this grouping;
otherwise all points are drawn in a single color.}
}
\value{
Invisibly returns a data frame of class \code{data.frame} whose
row names are the \code{rows} variables and column names are the \code{cols}
variables; each cell contains a single-element list with the corresponding
\code{ggplot} scatter object.
}
\description{
\code{scatter()} creates a grid of scatter plots for every combination of two
subsets of numeric variables, optionally colored by a categorical variable.
It prints a combined faceted plot (rows × columns) and invisibly returns a
data frame of the individual \code{ggplot} objects, indexed by row and column
variable names.
}
\details{
\itemize{
\item Numeric selections for \code{rows} and \code{cols} are handled via
\code{select_numeric()}.
\item The combined plot uses \code{ggplot2::facet_grid(row_v ~ col_v, scales = "free")}
so each scatter panel has its own axis ranges.
\item A light gray panel border is drawn around each facet.
\item If \code{cat_var} is supplied, a legend is shown; otherwise the color guide is
suppressed.
}
}
\examples{
# All numeric vs itself in mtcars
plots <- scatter(mtcars)

# Sepal vs Petal dimensions in iris, colored by Species
plots2 <- scatter(
  iris,
  rows    = c(Sepal.Length, Petal.Length),
  cols    = c(Sepal.Width,  Petal.Width),
  cat_var = Species
)

# Access the plot for Sepal.Length (row) vs Sepal.Width (col)
plots2["Sepal.Length", "Sepal.Width"]

}
