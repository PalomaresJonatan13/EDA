% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/barplots.R
\name{barplots}
\alias{barplots}
\title{Multiple Barplots for Categorical Variables}
\usage{
barplots(df, vars = NULL)
}
\arguments{
\item{df}{A data frame or tibble.}

\item{vars}{Optional unquoted tidy-select specification of categorical
columns to plot (e.g. \code{c(a, b, starts_with("x"))}).  If \code{NULL}
(default), all non-numeric columns in \code{df} are used.}
}
\value{
Invisibly returns a named list of \code{ggplot} objects, one per
variable.  The primary side effect is printing a combined faceted plot
of all barplots.
}
\description{
\code{barplots()} generates individual barplots for each selected non-numeric
(categorical) variable, arranges them in a composite faceted layout, and
returns the list of ggplot objects invisibly.
}
\details{
\itemize{
\item Each subplot shows counts of levels (including \code{NA}) for one variable.
\item Subplot titles are centered and individual y-axis labels are omitted
in favor of a shared count axis.
\item Composite layout is produced with \code{facet_wrap(~variable, scales = "free")}
and an approximately square grid: \verb{ncol = ceiling(sqrt(k)),}
\code{nrow = ceiling(k / ncol)}, where \code{k} is the number of variables.
}
}
\examples{
# All categorical variables in iris
bp <- barplots(iris)

# Specific factors in a custom data frame
df <- data.frame(
  gender  = sample(c("M", "F"), 50, TRUE),
  outcome = sample(c("yes", "no"), 50, TRUE),
  age     = sample(20:30,    50, TRUE)
)
bp2 <- barplots(df, c(gender, outcome))

}
