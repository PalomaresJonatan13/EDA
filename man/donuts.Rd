% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/donuts.R
\name{donuts}
\alias{donuts}
\title{Donut Charts for Categorical Variables}
\usage{
donuts(df, vars = NULL)
}
\arguments{
\item{df}{A data frame or tibble.}

\item{vars}{Optional unquoted tidy-select specification of columns to plot
(e.g. \code{c(a, b, starts_with("x"))}).  If \code{NULL} (default), all non-numeric
columns in \code{df} are used.}
}
\value{
Invisibly returns a named list of \code{ggplot} objects (one per variable).
The primary side effect is printing the combined grid of donut charts.
}
\description{
\code{donuts()} creates individual donut (ring-pie) charts for each selected
non-numeric (categorical) variable, arranges them in a grid via \strong{patchwork},
and returns the list of ggplot objects invisibly.
}
\details{
\itemize{
\item Each donut shows percentage segments for each level (including \code{NA}),
labeled with rounded percent values at the mid-arc position.
\item Inner hole is created by setting \code{xlim(0.5, 2.5)} after a \code{coord_polar()}.
\item Colours are drawn from the \strong{RColorBrewer} \code{"Set1"} palette.
\item Legends (level labels) appear to the right of each donut.
\item Composite layout uses \code{patchwork::wrap_plots()} with
\code{ncol = ceiling(sqrt(k))}, \code{nrow = ceiling(k / ncol)}, where \code{k} is the
number of variables.
}
}
\examples{
# All categorical variables in iris (only Species here)
donuts(iris)

# Specific variables in a custom data frame
df <- data.frame(
  gender  = sample(c("M", "F"), 50, TRUE),
  outcome = sample(c("yes", "no"), 50, TRUE),
  score   = rnorm(50)
)
donuts(df, c(gender, outcome))

}
