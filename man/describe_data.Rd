% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/describe_data.R
\name{describe_data}
\alias{describe_data}
\title{Comprehensive data diagnostics for numeric and categorical variables}
\usage{
describe_data(df, vars = NULL)
}
\arguments{
\item{df}{A data frame or tibble to summarize.}

\item{vars}{Optional unquoted tidy-select specification of columns to
include in the analysis (e.g. \code{c(a, b, starts_with("x"))}).  If \code{NULL},
\emph{all} columns are used.  Non-selected columns are ignored.}
}
\value{
A named list with components:
\describe{
\item{summary_numeric}{A tibble of numeric summaries per variable.}
\item{summary_missing}{A tibble of missing-data diagnostics.}
\item{summary_outlier}{A tibble of outlier diagnostics for numeric vars.}
\item{summary_category}{A tibble of frequency diagnostics for categorical vars.}
}
}
\description{
\code{describe_data()} computes four complementary summaries for a data frame:
\enumerate{
\item \strong{Numeric summary} (\code{summary_numeric}): mean, standard deviation,
variance, min, quartiles, max, kurtosis, skewness, IQR, and non-missing
count for each numeric variable.
\item \strong{Missing data summary} (\code{summary_missing}): counts and proportions of
missing values as returned by \code{dlookr::diagnose()}.
\item \strong{Outlier summary} (\code{summary_outlier}): outlier diagnostics for numeric
variables via \code{dlookr::diagnose_outlier()}.
\item \strong{Categorical summary} (\code{summary_category}): frequency and proportion
details for non-numeric variables via \code{dlookr::diagnose_category()}.
}
}
\examples{
# Describe all variables in iris
desc1 <- describe_data(iris)

# Describe only Sepal measurements
desc2 <- describe_data(iris, vars = c(Sepal.Length, Sepal.Width))

# Get summaries for mtcars engine variables
desc3 <- describe_data(mtcars, vars = c(mpg, hp, disp))

}
