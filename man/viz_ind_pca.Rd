% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_ind_pca.R
\name{viz_ind_pca}
\alias{viz_ind_pca}
\title{Visualize Individual Metrics from PCA}
\usage{
viz_ind_pca(pca_result, top = NULL, axes = NULL)
}
\arguments{
\item{pca_result}{A \code{FactoMineR::PCA} object, or a list containing a
\code{selected_components} element (e.g., output of \code{select_pca()}); the PCA
object is extracted from \code{selected_components} if present.}

\item{top}{Integer; number of top individuals to display in the bar charts
(\code{fviz_cos2} and \code{fviz_contrib}).  Defaults to all individuals if \code{NULL}.}

\item{axes}{Integer vector; the principal component axes to visualize.
Defaults to all available axes in the PCA result.}
}
\value{
Invisibly returns a named list of four plot objects:
\describe{
\item{cos2}{A ggplot object showing squared cosines (quality of
representation) of individuals on the specified axes
(\code{factoextra::fviz_cos2}).}
\item{contrib}{A ggplot object showing contributions of individuals to the
specified axes (\code{factoextra::fviz_contrib}).}
\item{cos2_corr}{A heatmap (corrplot) of the cos² values for individuals,
plotted as a matrix.}
\item{contrib_corr}{A heatmap (corrplot) of the contribution values for
individuals, plotted as a matrix.}
}
}
\description{
\code{viz_ind_pca()} generates diagnostic plots for the quality of representation
(cos²) and contributions of observations (individuals) in a PCA result,
including both bar charts and heatmaps.
}
\details{
\itemize{
\item If \code{pca_result} is a list containing \code{selected_components}, that element
is used as the PCA object.
\item The \code{top} argument limits the number of individuals highlighted in the
bar charts; if \code{NULL}, all individuals are shown.
\item The \code{axes} argument specifies which principal components to include in
both bar charts and heatmaps.
\item Heatmaps use \code{is.corr = FALSE} to display raw cos² and contribution values.
}
}
