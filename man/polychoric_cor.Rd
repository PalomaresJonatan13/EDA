% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polychoric_cor.R
\name{polychoric_cor}
\alias{polychoric_cor}
\title{Polychoric Correlation Matrix for Ordinal Variables}
\usage{
polychoric_cor(df, rows = NULL, cols = NULL, heatmap = FALSE)
}
\arguments{
\item{df}{A data frame or tibble containing the variables.}

\item{rows}{Unquoted tidy-select specification of columns to use as the \emph{y}
variables.  Variables must be ordinal (ordered factors) or factors
representing ordered categories.  If \code{NULL}, all non-numeric columns are used.}

\item{cols}{Unquoted tidy-select specification of columns to use as the \emph{x}
variables.  Variables must be ordinal or factors.  If \code{NULL}, all
non-numeric columns are used.}

\item{heatmap}{Logical; if \code{TRUE}, calls \code{heatmap_cor()} to display a tile-
based heatmap of the computed correlations before returning.}
}
\value{
A data frame whose row names correspond to the selected \code{rows}
variables and whose column names correspond to the selected \code{cols}
variables; each entry is the polychoric correlation coefficient.
}
\description{
\code{polychoric_cor()} computes pairwise polychoric correlation coefficients
between two sets of ordinal variables in a data frame, optionally displaying
a heatmap of the results.
}
\details{
\itemize{
\item \strong{Applicable pairs:} ordinal–ordinal only.
\item Estimates the correlation between unobserved continuous latent variables
underlying the observed ordinal outcomes via \code{polycor::polychor()}.
\item \strong{Assumptions:}
\enumerate{
\item The latent variables follow a bivariate normal distribution.
\item Observations are independent.
\item Each input variable is a true ordinal scale (ordered factor).
\item Adequate category frequencies; extremely sparse categories may yield
unstable estimates.
}
\item Missing values are handled pairwise: each correlation is computed on the
subset of rows where both variables are non-missing.
}
}
