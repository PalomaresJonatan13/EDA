% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/level_boxplots.R
\name{level_boxplots}
\alias{level_boxplots}
\title{Boxplots by Categorical Levels with Summary Statistics}
\usage{
level_boxplots(df, num_var, cat_var, horizontal = FALSE)
}
\arguments{
\item{df}{A data frame or tibble.}

\item{num_var}{Unquoted name of a numeric column in \code{df} to plot.}

\item{cat_var}{Unquoted name of a non-numeric (categorical) column in \code{df}
used to define levels.}

\item{horizontal}{Logical; if \code{FALSE} (default), boxplots are vertical
(\verb{x = level, y = value}); if \code{TRUE}, they are horizontal
(\verb{x = value, y = level}).}
}
\value{
Invisibly returns a list with elements:
\describe{
\item{boxplots}{A named list of \code{ggplot} boxplot objects, one per level.}
\item{stats}{A tibble of descriptive statistics (mean, sd, etc.) for each
level, with a \code{category} column.}
}
}
\description{
\code{level_boxplots()} creates boxplots of a numeric variable stratified by each
level of a categorical variable.  All level‐specific boxplots share one
combined display, and individual boxplots plus descriptive statistics are
returned invisibly.
}
\details{
\itemize{
\item Requires that \code{num_var} selects a numeric column and \code{cat_var} selects a
non-numeric (factor or character) column, otherwise an error is raised.
\item The combined plot shows boxplots for all levels in one panel.
\item Summary statistics are computed via \code{describe_data()} and include only
the numeric summary table.
}
}
\examples{
# Vertical boxplots of Sepal.Length by Species in iris
level_boxplots(iris, Sepal.Length, Species)

# Horizontal boxplots of Petal.Width by Species
level_boxplots(iris, Petal.Width, Species, horizontal = TRUE)

}
