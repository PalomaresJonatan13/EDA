% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boxplots.R
\name{boxplots}
\alias{boxplots}
\title{Multiple Boxplots with Patchwork Layout}
\usage{
boxplots(df, vars = NULL)
}
\arguments{
\item{df}{A data frame or tibble.}

\item{vars}{Optional unquoted tidy-select specification of numeric columns to
plot (e.g. \code{c(a, b, starts_with("x"))}).  If \code{NULL} (default), all numeric
columns in \code{df} are plotted.}
}
\value{
Invisibly returns a named list of \code{ggplot} objects, one per variable.
The primary side effect is printing the combined grid of boxplots.
}
\description{
\code{boxplots()} generates individual boxplots for each selected numeric variable,
arranges them in an approximately square grid using \strong{patchwork}, and returns
the list of ggplot objects invisibly.
}
\details{
\itemize{
\item Each subplot is a boxplot (no outlier labels) with a centered title (font
size = 8).
\item Subplots share no common axis beyond the vertical scale per facet.
\item Composite layout uses \code{patchwork::wrap_plots()} with \code{ncol = ceiling(sqrt(k))}
and \code{nrow = ceiling(k / ncol)}, where \code{k} is the number of variables.
}
}
\examples{
# All numeric variables in mtcars
plots <- boxplots(mtcars)

# Subset of variables
plots2 <- boxplots(iris, vars = c(Sepal.Length, Sepal.Width))

}
