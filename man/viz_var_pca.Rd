% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_var_pca.R
\name{viz_var_pca}
\alias{viz_var_pca}
\title{Visualize Variable Metrics from PCA}
\usage{
viz_var_pca(pca_result, top = NULL, axes = NULL)
}
\arguments{
\item{pca_result}{A \code{FactoMineR::PCA} object, or a list containing a
\code{selected_components} element (e.g., output of \code{select_pca()}) from which
the PCA object will be extracted.}

\item{top}{Integer; the number of top variables to display in the bar
charts (\code{fviz_cos2} and \code{fviz_contrib}).  Defaults to all variables.}

\item{axes}{Integer vector; the principal component axes to visualize.
Defaults to all available axes in the PCA result.}
}
\value{
Invisibly returns a named list of four plot objects:
\describe{
\item{cos2}{A ggplot object displaying squared cosines of variables on the
specified axes (via \code{factoextra::fviz_cos2}).}
\item{contrib}{A ggplot object showing variable contributions to the
specified axes (via \code{factoextra::fviz_contrib}).}
\item{cos2_corr}{A heatmap (corrplot) of the cos² values as a matrix plot.}
\item{contrib_corr}{A heatmap (corrplot) of the contribution values as a
matrix plot.}
}
}
\description{
\code{viz_var_pca()} generates multiple diagnostic plots for variable-level
metrics from a PCA result: quality of representation (cos²) and variable
contributions, each as both bar charts and correlation-matrix heatmaps.
}
\details{
\itemize{
\item If \code{pca_result} is a list and contains \code{selected_components}, that element
is used; otherwise, \code{pca_result} itself must be a PCA object.
\item The \code{top} argument limits the number of variables highlighted in the bar
plots; if \code{NULL}, all variables are shown.
\item The \code{axes} argument selects which principal components to include in the
bar charts and heatmaps.
\item Heatmaps are rendered with \code{is.corr = FALSE}, so values are shown directly
rather than treated as correlation coefficients.
}
}
