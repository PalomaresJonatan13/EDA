% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/corr_circle_pca.R
\name{corr_circle_pca}
\alias{corr_circle_pca}
\title{PCA Correlation Circle Plots for Variables and Individuals}
\usage{
corr_circle_pca(pca_result, axes = c(1, 2))
}
\arguments{
\item{pca_result}{A \code{FactoMineR::PCA} object, or a list containing a
\code{selected_components} element (e.g., output of \code{select_pca()}), from which
the PCA object will be extracted if present.}

\item{axes}{Integer vector of length two giving the principal component
axes to plot (e.g., \code{c(1, 2)} for the first two dimensions).}
}
\value{
A named list of six \code{ggplot} objects:
\describe{
\item{var}{Correlation circle for variables, points colored solid.}
\item{var_cos2}{Correlation circle for variables, colored by squared cosines
(quality of representation).}
\item{var_contrib}{Correlation circle for variables, colored by variable
contributions.}
\item{ind}{Correlation circle for individuals, points colored solid.}
\item{ind_cos2}{Correlation circle for individuals, colored by squared
cosines (quality of representation).}
\item{ind_contrib}{Correlation circle for individuals, colored by
contributions.}
}
}
\description{
\code{corr_circle_pca()} produces a list of correlation circle visualizations for
both variables and individuals from a PCA result, colored by metric
(none, cos², or contribution) and displayed on specified principal component
axes.
}
\details{
\itemize{
\item Uses \code{factoextra::fviz_pca_var()} and \code{factoextra::fviz_pca_ind()}
with \code{repel = TRUE} to avoid label overlap.
\item Color gradients for cos² and contribution are defined by the palette
\code{c("#00afbb", "#e7b800", "#fc4e07")}.
\item The variable plots (\verb{var*}) display variable loadings as vectors on the
correlation circle; the individual plots (\verb{ind*}) show observation
projections.
}
}
